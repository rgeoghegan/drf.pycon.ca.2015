!!! html
%html
  %head
    %meta{:charset => "utf-8"}
    %title Blah
    - include_css "css/reveal.css"
    - include_css "css/theme/moon.css"
    - include_css "lib/css/zenburn.css"

    - include_css "paper.css"
    - include_css "pdf.css"
    - include_css "lib/font/league-gothic/league-gothic.css"
  %body
    .reveal
      .slides
        %section
          Hello

        %section
          %h1 Unicode is ð© in python 2 but better in python 3

        %section
          %h1 Unicode is &#x1f4a9; in python 2 but better in python 3

        %section.all_code
          %p
            %code >>> in_unicode = u'\u2603'
          %p
            %code.fragment >>> print(in_unicode)
          %p
            %code.fragment &#x2603;
          %p
            %code.fragment >>> len(in_unicode)
          %p
            %code.fragment 1

        %section
          %h2 Pretty Code
          %pre
            %code
              :preserve
                function linkify( selector ) {
                  if( supports3DTransforms ) {
                
                    var nodes = document.querySelectorAll( selector );
                
                    for( var i = 0, len = nodes.length; i &lt; len; i++ ) {
                      var node = nodes[i];
                
                      if( !node.className ) {
                        node.className += ' roll';
                      }
                    }
                  }
                }

    - include_js "lib/js/head.min.js"
    - include_js "js/reveal.js"
    - include_js "init.js"
